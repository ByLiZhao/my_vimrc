# configuration file for YouCompleteMe with Cpp
def Settings(**kwargs):
    return {
        'flags': [ '-x', 'c', \
            '-Wall', '-Wextra', \
            '-fwrapv', '-fno-delete-null-pointer-checks', '-pthread', \
            '-I', 'src', \
            '-I', 'include', \
            ],
    }
