# configuration file for YouCompleteMe with Cpp 
def Settings( **kwargs ):
  return {
      'flags': [ '-x', 'c', \
          '-Wall', '-Wextra', '-pedantic', \
          '-std=c11', '-pthread', \
          '-I', 'src', \
          '-I', 'include', \
          '-isystem', '/usr/include', \
          '-isystem', '/usr/local/include',],
  }
